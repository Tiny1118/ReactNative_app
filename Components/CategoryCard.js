/* import { View, Text, ScrollView, TouchableOpacity, Image } from 'react-native'
import React from 'react'

const CategoryCard = ({ imgURl, title }) => {
  return (
    <TouchableOpacity style={{ width: 150, height: 150, marginRight: 20, borderRadius: 10, position: 'relative'}}>
      <Image 
       source={{
        uri: imgURl
       }}
       style={{ width: '100%', height: '100%', borderRadius: 10 }}
      />
      <Text style={{ position: 'absolute', bottom: 10, left: 10, color: '#fff' }}>{title}</Text>
    </TouchableOpacity>
  )
}

export default CategoryCard */